Guess the output??

#include <bits/stdc++.h>
using namespace std;

void fun(int, string name = "NA");

int main() {

	fun(101);
  
	return 0;
}

void fun(int id, string name){
    cout << id << " " << name;
}

Ans -> compiler error(linker error)
we define function but not declared it ,so it will not found any function
